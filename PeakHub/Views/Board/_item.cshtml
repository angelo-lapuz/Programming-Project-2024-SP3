@model PeakHub.Models.Board

<div data-id="@Model.BoardID" class="itemBox">
    <img src="@Model.ImageLink" alt="Board's Header Image" />

    <div class="itemData">
        <p> <strong> @Model.Name </strong> </p>
        <p> <i> @Model.Section </i> </p>
    </div>
</div>